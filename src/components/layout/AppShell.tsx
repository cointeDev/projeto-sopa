/*
import type { ReactNode } from "react";

type AppShellProps = {
  left: ReactNode;
  right?: ReactNode;
};

export function AppShell({ left, right }: AppShellProps) {
  return (
    <div className="min-h-screen grid grid-cols-1 lg:grid-cols-[35%_65%] bg-sopa-dark font-inter">
      <section className="relative flex flex-col h-full bg-sopa-panel px-6 py-12 shadow-sopa-panel after:absolute after:inset-y-0 after:right-0 after:w-px after:bg-white/5">
        {left}
      </section>

      {right && (
        <div className="relative hidden lg:block">
          {right}
        </div>
      )}
    </div>
  );
}
*/