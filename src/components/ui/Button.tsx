/*
import { Link } from "@tanstack/react-router";
import type { ReactNode } from "react";

type ButtonProps = {
  to: string;
  children: ReactNode;
};

export function PrimaryCTA({ to, children }: ButtonProps) {
  return (
    <Link
      to={to}
      className="flex items-center justify-center gap-3 w-full mb-4
                 rounded-xl px-6 py-5 font-bold text-white
                 bg-sopa-primary hover:bg-sopa-primary-hover
                 shadow-sopa-glow
                 hover:-translate-y-1 hover:shadow-sopa-glow-strong
                 transition-all"
    >
      {children}
    </Link>
  );
}

export function SecondaryButton({ to, children }: ButtonProps) {
  return (
    <Link
      to={to}
      className="flex-1 text-center rounded-lg border border-white/20
                 py-3 text-sm font-semibold text-white
                 hover:border-white hover:bg-white/10 transition"
    >
      {children}
    </Link>
  );
}
*/